// Public-facing layout (Home, Shop, Lookbook, About, Contact)
// Navbar and Footer will be added here once components are built
export default function PublicLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return <>{children}</>;
}
